{/*
import { NavigationContainer } from "@react-navigation/native";
import { createDrawerNavigator } from '@react-navigation/drawer';
import React from "react";
// import ClassesScreen from ""
// import AssignmentsScreen from "";
// import StudentsInClassScreen from "";
import TeacherSubscription from "../../screens/teacher_subscription";

export type SideBarList = {
    // Classes: undefined;
    // Assignments: undefined;
    TeacherSubscription: undefined;
    // StudentsInClass: undefined;
};

const Drawer = createDrawerNavigator<SideBarList>();

function TeacherStack () {
  return (
    <NavigationContainer independent={true}>
      <Drawer.Navigator initialRouteName="TeacherSubscription">
        {/*
        <Drawer.Screen name = "Classes" component={ClassesScreen} />
        <Drawer.Screen name = "Assignments" component={AssignmentsScreen} />
         <Drawer.Screen name = "StudentsInClass" component={StudentsInClassScreen} />
        <Drawer.Screen name = "TeacherSubscription" component={TeacherSubscription} />
      </Drawer.Navigator>
    </NavigationContainer>
  );
}

export default TeacherStack;

*/}